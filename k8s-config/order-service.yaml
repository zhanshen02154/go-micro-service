apiVersion: v1
kind: Service
metadata: 
  name: order-service
  namespace: dev
  labels: 
    srv: order-service
spec:
  selector:
    app: order-service
  ports:
  - name: grpc
    port: 9000        # Service对外暴露的端口
    targetPort: 9000  # 指向Pod内容器端口
    protocol: TCP     # 必须明确指定为TCP
  type: ClusterIP     # 适用于集群内部服务间调用